// this file is not used anywhere, nor included. It exists only because
// `django-admin` seems to forget some gettext'ed message for whatever reason.
// We thus assemble them here again, for them to be picked up correctly.

gettext("Confirm");
gettext("Cancel");
gettext("Close");
