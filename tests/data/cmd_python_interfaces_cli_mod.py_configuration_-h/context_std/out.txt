Usage: 
	[01;33mmod.py[0;0m config[uration] [--hide-groups|--set-hidden-groups|--unhide-groups|-u|-U] [--set-hostname <new hostname>] [--restrictive] [--set-ip-address <NEW.ETH0.IP.ADDR>]

Options:
  --version             show program's version number and exit
  -h, --help            show this help message and exit

  [00;34mBehavior options [0;0m:
    Modify decisions / apply filter(s) on data manipulated by program.

    -f, --force         forces the current action (if applicable).
    -q, --quiet         be quiet, don't display anything except on
                        warnings/errors.
    -v, --verbose       be more verbose (in command-line mode) ; you can get
                        more with -vv, -vvv, etc.[01;37mthe default is INFO
                        level of verbosity[0;0m, which is quite moderated.
                        You will get information about auto-generated
                        passwords and long-time actions, when you should
                        expect to wait for them to complete.
    --no-colors         no colors in any messages (CLI mode only).

  [00;34mModify configuration options [0;0m:
    --setup-shared-dirs
                        create system groups, directories and settings from
                        system configuration (PARTIALLY OBSOLETED, TOTALLY
                        UNSUPPORTED AND VERY DANGEROUS, USE WITH CAUTION).
    -r, --restrictive, --set-restrictive
                        when creating system groups and directories, apply
                        restrictive perms (710) on shared dirs instead of
                        relaxed ones (750).
    -u, --hide-groups, --set-groups-hidden
                        Set restrictive perms (710) on
                        [00;34m/home/groups[0;0m.
    -U, --unhide-groups, --set-groups-visible
                        Set relaxed perms (750) on [00;34m/home/groups[0;0m.
    -b ENABLE_BACKENDS, --enable-backends=ENABLE_BACKENDS
                        enable given backend(s) on the current system
                        (separated by commas without spaces). List of
                        available backends with `[00;32mget config
                        backends[0;0m`.
    -B DISABLE_BACKENDS, --disable-backends=DISABLE_BACKENDS
                        disable given backend(s) on the current system
                        (separated by commas without spaces). List of
                        available backends with `[00;32mget config
                        backends[0;0m`.
    -e SET_HOSTNAME, --set-hostname=SET_HOSTNAME
                        change machine hostname.
    -i SET_IP_ADDRESS, --set-ip-address=SET_IP_ADDRESS
                        change machine's IP (for eth0 only).
    --add-privileges=PRIVILEGES_TO_ADD
                        add privileges (system groups) to privileges
                        whitelist.
    --remove-privileges=PRIVILEGES_TO_REMOVE
                        remove privileges (system groups) from privileges
                        whitelist.
