Usage: 
	[01;33madd.py[0;0m profile [--name=]<name> [-g|--group=<groupName>] [--description=<descr>]
		[--shell=<shell>] [--quota=<quota>] [--skel=<nom_squelette>]
		[-a|--[add-]groups=<groupe1>[[,groupe2][,...]] [--force-existing]

Options:
  --version             show program's version number and exit
  -h, --help            show this help message and exit

  [00;34mBehavior options [0;0m:
    Modify decisions / apply filter(s) on data manipulated by program.

    -f, --force         forces the current action (if applicable).
    -q, --quiet         be quiet, don't display anything except on
                        warnings/errors.
    -v, --verbose       be more verbose (in command-line mode) ; you can get
                        more with -vv, -vvv, etc.[01;37mthe default is INFO
                        level of verbosity[0;0m, which is quite moderated.
                        You will get information about auto-generated
                        passwords and long-time actions, when you should
                        expect to wait for them to complete.
    --no-colors         no colors in any messages (CLI mode only).

  [00;34mAdd profile options [0;0m:
    --name=NAME         The profile's name (ie: «Administrator», «Power
                        user», «Webmaster», «Guest»). It should be a
                        singular word and [01;31mit is required[0;0m.
    -g GROUP, --group=GROUP
                        Group name identifying the profile on the system (ie:
                        «administrators», «power-users», «webmasters»,
                        «guests»). It should be a plural world and will
                        become a system group. [01;31mIt is required[0;0m.
    --description=DESCRIPTION
                        Description of the profile (free text).
    --shell=SHELL       Default shell for this profile (defaults to
                        [01;37m/bin/bash[0;0m).
    --quota=QUOTA       User data quota in Mb (soft quota, defaults to
                        [01;37m1024[0;0m).
    -a GROUPS, --groups=GROUPS, --add-groups=GROUPS
                        Groups users of this profile will become members of.
                        Separated by commas without spaces.
    --skel=SKELDIR      skeleton dir for this profile (must be an absolute
                        path, defaults to [01;37m/etc/skel[0;0m).
    --force-existing    Confirm the use of a previously created system group
                        for the profile. [01;31mThis is risky[0;0m, but in
                        some cases (where the group is created by another
                        package or script) this is OK.
