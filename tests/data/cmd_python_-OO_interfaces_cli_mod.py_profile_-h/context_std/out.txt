Usage: 
	[01;33mmod.py[0;0m profile --group=<nom> [--name=<nouveau_nom>] [--rename-group=<nouveau_nom>]
		[--comment=<nouveau_commentaire>] [--shell=<nouveau_shell>] [--skel=<nouveau_skel>]
		[--quota=<nouveau_quota>] [--add-groups=<groupes>] [--del-groups=<groupes>]
	[01;33mmod.py[0;0m profile <--apply-groups|--apply-skel|--apply-all> [--force]
		[--to-users=<user1[[,user2][,...]]>] [--to-groups=<group1[[,group2][,...]]>]
		[--to-all] [--to-members] [--no-instant-apply] [--no-sync]

Options:
  --version             show program's version number and exit
  -h, --help            show this help message and exit

  [00;34mBehavior options [0;0m:
    Modify decisions / apply filter(s) on data manipulated by program.

    -f, --force         forces the current action (if applicable).
    -q, --quiet         be quiet, don't display anything except on
                        warnings/errors.
    -v, --verbose       be more verbose (in command-line mode) ; you can get
                        more with -vv, -vvv, etc.[01;37mthe default is INFO
                        level of verbosity[0;0m, which is quite moderated.
                        You will get information about auto-generated
                        passwords and long-time actions, when you should
                        expect to wait for them to complete.
    --no-colors         no colors in any messages (CLI mode only).

  [00;34mModify profile options [0;0m:
    --group=GROUP, --name=GROUP
                        specify profile to modify by its primary group
                        ([01;31mrequired[0;0m).
    --rename=NEWNAME    specify profile's name
    --rename-group=NEWGROUP
                        Rename primary group.
    --description=DESCRIPTION
                        Change profile's description.
    --shell=NEWSHELL    Change profile shell (defaults to
                        [01;37m/bin/bash[0;0m if you specify --shell without
                        argument)
    --quota=NEWQUOTA    Change profile's user quota (in Mb, defaults to
                        [01;37m1024[0;0m if you specify --quota without
                        argument).
    --skel=NEWSKEL      Change profile skel (specify a skel dir as an absolute
                        pathname, defaults to [01;37m/etc/skel[0;0m if you
                        give --skel without argument).
    --add-groups=GROUPS_TO_ADD
                        Add one or more group(s) to default memberships of
                        profile (separate groups with commas without spaces).
    --del-groups=GROUPS_TO_DEL
                        Delete one or more group(s) from default memberships
                        of profile (separate groups with commas without
                        spaces).
    --apply-groups      Re-apply only the default group memberships of the
                        profile.
    --apply-skel        Re-apply only the skel of the profile.
    --apply-all         Re-apply all the profile's attributes (groups and
                        skel).
    --to-users=APPLY_TO_USERS
                        Re-apply to specific users accounts (separate them
                        with commas without spaces).
    --to-groups=APPLY_TO_GROUPS
                        Re-apply to all members of one or more groups
                        (separate groups with commas without spaces). You can
                        mix --to-users and --to-groups.
    --to-members        Re-apply to all users members of the profile.
    --to-all            Re-apply to all user accounts on the system (LENGHTY
                        operation !).
    --no-instant-apply  Don't apply group addition/deletion instantly to all
                        members of the modified profile ([01;31mthis is not
                        recommended[0;0m; use this only if you know what
                        you're doing).
    --no-sync           Commit changes only after all modifications.
