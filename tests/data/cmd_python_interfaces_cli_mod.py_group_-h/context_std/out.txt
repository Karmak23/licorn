Usage: 
	[01;33mmod.py[0;0m group --name=<nom_actuel> [--rename=<nouveau_nom>]
		[--add-users=<user1[[,user2][,...]]>] [--del-users=<user1[[,user2][,...]]>]
		[--add-resps=<user1[[,user2][,...]]>] [--delete-resps=<user1[[,user2][,...]]>]
		[--add-guests=<user1[[,user2][,...]]>] [--delete-guests=<user1[[,user2][,...]]>]
		[--permissive|--not-permissive] [--skel=<new skel>] [--description=<new description>]

Options:
  --version             show program's version number and exit
  -h, --help            show this help message and exit

  [00;34mBehavior options [0;0m:
    Modify decisions / apply filter(s) on data manipulated by program.

    -f, --force         forces the current action (if applicable).
    -q, --quiet         be quiet, don't display anything except on
                        warnings/errors.
    -v, --verbose       be more verbose (in command-line mode) ; you can get
                        more with -vv, -vvv, etc.[01;37mthe default is INFO
                        level of verbosity[0;0m, which is quite moderated.
                        You will get information about auto-generated
                        passwords and long-time actions, when you should
                        expect to wait for them to complete.
    --no-colors         no colors in any messages (CLI mode only).

  [00;34mModify group options [0;0m:
    --name=NAME         specify group's name to modify
                        ([01;31mrequired[0;0m).
    --rename=NEWNAME    specify group's new name (not yet implemented).
    --skel=NEWSKEL      specify group's new skel dir.
    --description=NEWDESCRIPTION
                        specify new group's description
    -p, --permissive, --set-permissive
                        set the shared directory of the group permissive.
    -P, --not-permissive, --set-not-permissive
                        set the shared directory of the group not permissive.
    --add-users=USERS_TO_ADD
                        Add users to the group. The users are separated by
                        commas without spaces.
    --del-users=USERS_TO_DEL
                        Delete users from the group. The users are separated
                        by commas without spaces.
    --add-resps=RESPS_TO_ADD
                        Add responsibles to the group. The responsibles are
                        separated by commas without spaces.
    --del-resps=RESPS_TO_DEL
                        Delete responsibles from the group. The responsibles
                        are separated by commas without spaces.
    --add-guests=GUESTS_TO_ADD
                        Add guests to the group. The guests are separated by
                        commas without spaces.
    --del-guests=GUESTS_TO_DEL
                        Delete guests from the group. The guests are separated
                        by commas without spaces.
    --add-granted-profiles=GRANTED_PROFILES_TO_ADD
                        Add the profiles which the users can access to the
                        group's shared directory. The profiles are separated
                        by commas without spaces.
    --del-granted-profiles=GRANTED_PROFILES_TO_DEL
                        Delete the profiles which the users can access to the
                        group's shared directory. The profiles are separated
                        by commas without spaces.
