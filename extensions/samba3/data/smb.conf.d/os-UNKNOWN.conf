# By default, UNKNOWN OS get the wide links parameters. It's already common
# to all know Windows version, so it's a sane default assumption that a new
# OS will be another variant of an MS operating system.

include = /etc/samba/smb.conf.d/wide-links.conf
